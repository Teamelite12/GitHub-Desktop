name: Dummy Test Trigger

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  dummy-test:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy step
        run: echo "Dummy workflow triggered successfully"
